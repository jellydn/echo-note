---

## [Sun Apr 6 2025 08:00:00 UTC] - US-011
- What was implemented:
  - Fixed the API fallback endpoint URL construction bug
  - Modified `generate_summary_command` in `src-tauri/src/lib.rs` to construct the full API URL
  - Added logic to append `/chat/completions` to base URLs from settings
  - Handles multiple URL formats correctly:
    - Already-complete URLs (ends with `/chat/completions`) - used as-is
    - Base URLs ending with `/v1` - appends `/chat/completions`
    - URLs with trailing slash - appends `chat/completions`
    - URLs without trailing slash - appends `/chat/completions`
  - This fixes the issue where the default `https://api.openai.com/v1` was being used directly instead of `https://api.openai.com/v1/chat/completions`
- Files changed:
  - `src-tauri/src/lib.rs` - Updated `generate_summary_command` to construct full API URL (lines 849-866)
- Quality checks passed:
  - TypeScript typecheck: `tsc --noEmit` ✓
  - Biome lint: `bun run lint` ✓
  - Rust typecheck: `cargo check` (in src-tauri) ✓
  - Rust lint: `cargo clippy` (in src-tauri) ✓
- **Learnings for future iterations:**
  - **API URL construction**: When using OpenAI-compatible APIs, always ensure the full endpoint path (`/chat/completions`) is included
  - **URL normalization**: Handle multiple user input formats gracefully - users may enter base URLs, full URLs, or URLs with/without trailing slashes
  - **Default values**: The default endpoint `https://api.openai.com/v1` is a base URL, not a complete API endpoint - application code must handle the transformation
  - **Configuration vs Runtime**: Settings storage should keep the base URL (more flexible), but runtime must construct the complete endpoint URL
  - **Pattern**: Check if URL already has the required path before appending to support both base URLs and full URLs
- What was implemented:
  - Fixed the post-recording navigation regression
  - Updated `App.tsx` `onMeetingCreated` callback to navigate to `meeting-detail` view instead of `history`
  - The callback now correctly sets `selectedMeetingId` before navigating, matching the pattern used in HistoryView's `onMeetingClick`
  - This fulfills the PRD requirement: "On completion, navigate to the meeting detail view"
- Files changed:
  - `src/App.tsx` - Modified `onMeetingCreated` callback in the RecordView case (lines 18-23)
- Quality checks passed:
  - TypeScript typecheck: `tsc --noEmit` ✓
  - Biome lint: `bun run lint` ✓
  - Rust typecheck: `cargo check` (in src-tauri) ✓
  - Rust lint: `cargo clippy` (in src-tauri) ✓
- **Learnings for future iterations:**
  - **Navigation pattern**: When navigating to a detail view, always set the ID state before changing the view state
  - **Callback prop consistency**: The `onMeetingCreated` callback in RecordView passes `meetingId: number`, but App.tsx was ignoring it - always verify callback signatures match implementation
  - **State ordering**: Set `selectedMeetingId(meetingId)` before `setCurrentView("meeting-detail")` to avoid race conditions where the view renders before the ID is available
  - **Pattern to follow**: See `HistoryView.onMeetingClick` for the correct navigation pattern to detail views
- What was implemented:
  - Fixed the audio device selection bug where non-default selections fell back to default
  - Changed `list_audio_devices()` in `src-tauri/src/audio/mod.rs` to use actual CoreAudio device names as IDs
  - Previously returned synthetic IDs like "device_0", "device_1" which didn't match actual device names during recording
  - Now both id and name are the actual device name from cpal, ensuring the stored setting matches what the recording thread expects
  - The "default" option still works as a special case that triggers `host.default_input_device()`
- Files changed:
  - `src-tauri/src/audio/mod.rs` - Modified `list_audio_devices()` to return device names as IDs
- Quality checks passed:
  - Rust typecheck: `cargo check` ✓
  - Rust lint: `cargo clippy` ✓
  - Rust format: `cargo fmt --check` ✓
  - TypeScript typecheck: `tsc --noEmit` ✓
  - Biome lint: `bun run lint` ✓
- **Learnings for future iterations:**
  - **Device ID pattern**: Use actual device names from cpal as IDs, not synthetic indices
  - **Why synthetic IDs fail**: Device order/index can change between enumeration and recording, causing mismatches
  - **CoreAudio naming**: Device names are stable identifiers on macOS (e.g., "MacBook Pro Microphone")
  - **Special "default" handling**: Keep a special "default" ID that maps to `host.default_input_device()` for user convenience
  - **Matching logic**: The recording thread already had partial/fuzzy matching, but exact name matching is more reliable
  - **State consistency**: Frontend stores device ID → Backend retrieves by ID → Recording thread matches by name
